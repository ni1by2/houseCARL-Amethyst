# Codex skill metadata for the houseCARL umbrella skill. Schema first reviewed 2026-06-04; re-verified
# 2026-07-21 against Codex's Build-skills docs (interface/policy/dependencies fields, ~/.agents/skills
# discovery, MCP-via-config.toml) — still current.
interface:
  display_name: "houseCARL-Amethyst"
  short_description: "Skyrim SE / Amethyst data-layer modding."
  default_prompt: "Use $housecarl to inspect or patch my Amethyst Skyrim load order."

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "housecarl"
      description: "houseCARL MCP server registered as [mcp_servers.housecarl] in ~/.codex/config.toml."
